# 📊 Анализ проекта PolyMarket для интеграции Markets API

**Дата анализа:** 03.10.2025  
**URL:** http://localhost:3002

---

## 🎯 Цель

Интегрировать реальные события из Markets API вместо замоканных данных.

---

## 📁 Структура проекта

### Технологический стек

- **Framework:** React 18 + TypeScript
- **Build Tool:** Vite 6.0.4
- **Styling:** Tailwind CSS 3.4.16
- **UI Components:** Shadcn/ui + Radix UI
- **Routing:** React Router DOM 6.8.1
- **Icons:** Lucide React

### Ключевые файлы

```
PolyMarket/
├── src/
│   ├── App.tsx                          # Главный компонент приложения
│   ├── screens/
│   │   ├── Desktop/
│   │   │   ├── Desktop.tsx              # Главная страница
│   │   │   └── sections/
│   │   │       ├── MainContentSection/  # Header с категориями
│   │   │       ├── QuestionCardSection/ # 🎯 Карточки событий (ЗАМОКАННЫЕ ДАННЫЕ)
│   │   │       └── StatisticsSection/   # Статистика
│   │   ├── CategoryPage/                # Страница категории
│   │   └── EventDetailsPage/            # Детальная страница события
│   └── components/
│       └── ui/                          # UI компоненты (button, card, etc)
```

---

## 🔍 Замоканные данные

### Местонахождение

**Файл:** `src/screens/Desktop/sections/QuestionCardSection/QuestionCardSection.tsx`

**Строки:** 28-533 (массив `mainQuestions`)

### Структура данных события

```typescript
interface MockedEvent {
  id: string;                    // 'economy-0', 'politics-1', etc
  image: string;                 // URL изображения (Pexels)
  title: string;                 // Название события
  yesPercentage: number;         // Процент "Да"
  noPercentage: number;          // Процент "Нет"
  chartData: string;             // SVG polyline points для графика
  chartColor: string;            // Цвет графика (#b2d33a, #ffa500, #ff6b6b)
  poolAmount: string;            // Призовой фонд ("₹4,25,000")
  deadline: string;              // Дедлайн ("Dec 31, 2025")
  category: string;              // Категория (economy, politics, tech, etc)
  type: 'binary' | 'multiple';  // Тип события
  options?: Array<{              // Для multiple choice
    label: string;
    percentage: number;
  }>;
}
```

### Количество событий

**Всего: 36 событий**

По категориям:
- 🏦 Economy: 6 событий
- 🏛️ Politics: 6 событий  
- 🌱 Sustainability: 6 событий
- 💻 Technology: 6 событий
- 🎭 Culture: 6 событий
- ⚽ Sports: 6 событий

---

## 🔄 Структура Markets API

### Endpoint

```
POST https://connect.markets.inout.games/api/markets
```

### Структура ответа API

```typescript
interface APIResponse {
  pagination: {
    page: number;
    limit: number;
    total: number;
  };
  markets: Array<{
    id: string;                    // UUID
    name: string;                  // Название
    slug: string;                  // URL slug
    image: string;                 // URL изображения
    status: string;                // "PENDING", "ACTIVE", "CLOSED"
    outcomes: Array<{
      id: string;
      name: string;                // "Да", "Нет"
      rate: number;                // 0.0 - 1.0
      resolved: boolean;
      created_at: number;          // Unix timestamp
    }>;
    created_at: number;            // Unix timestamp
    expired_at?: number;           // Unix timestamp
  }>;
}
```

---

## 📋 Маппинг данных

### Что нужно трансформировать

| Поле PolyMarket | Поле Markets API | Трансформация |
|----------------|------------------|---------------|
| `id` | `id` | Прямой маппинг |
| `title` | `name` | Прямой маппинг |
| `image` | `image` | Прямой маппинг |
| `yesPercentage` | `outcomes[0].rate * 100` | Конвертация в проценты |
| `noPercentage` | `outcomes[1].rate * 100` | Конвертация в проценты |
| `poolAmount` | ❌ Нет в API | Генерировать случайно или убрать |
| `deadline` | `expired_at` | Форматировать timestamp |
| `category` | ❌ Нет в API | Определять по keywords или дефолт |
| `chartData` | ❌ Нет в API | Генерировать на основе rate |
| `chartColor` | ❌ Нет в API | Вычислять по yesPercentage |
| `type` | `outcomes.length` | binary если 2, иначе multiple |
| `options` | `outcomes` (если > 2) | Маппить для multiple choice |
| `status` | `status` | Маппить PENDING→active, etc |

---

## 🎨 Особенности UI

### Типы карточек

1. **Binary (Да/Нет)**
   - Прогресс бар
   - Две кнопки (Yes/No)
   - Расчет потенциального дохода (x4-20)

2. **Multiple Choice**
   - Список опций с прогресс барами
   - Проценты по каждой опции

### График

- SVG polyline chart
- Цвета зависят от процентов:
  - `#b2d33a` (зеленый) - >60%
  - `#ffa500` (оранжевый) - 40-60%
  - `#ff6b6b` (красный) - <40%

### Категории

- all (все события)
- economy
- politics
- sustainability
- tech (или technology)
- culture
- sports

---

## 🚀 План интеграции

### Этап 1: Создание API клиента

```typescript
// src/lib/marketsAPI.ts
- Функция fetchMarkets()
- Обработка ошибок
- Кеширование (опционально)
```

### Этап 2: Трансформация данных

```typescript
// src/lib/transformMarkets.ts
- Функция transformAPIToMarket()
- Маппинг полей
- Генерация chartData
- Определение категорий
```

### Этап 3: Замена в компонентах

**QuestionCardSection.tsx:**
- Убрать массив `mainQuestions`
- Добавить `useState` для хранения данных
- Добавить `useEffect` для загрузки
- Обработка loading/error состояний

**EventDetailsPage.tsx:**
- Загрузка события по ID
- Обработка не найденных событий

### Этап 4: Fallback данные

- Оставить моковые данные как fallback
- Использовать при ошибке API
- Показывать индикатор "демо данные"

---

## ⚠️ Потенциальные проблемы

### 1. Отсутствие данных в API

**Проблема:** API может вернуть 0 events (сейчас так и есть на stage)

**Решение:** 
- Использовать моковые данные как fallback
- Добавить индикатор режима (API / Mock)

### 2. Категории отсутствуют в API

**Проблема:** API не возвращает категорию события

**Решение:**
- Определять по keywords в названии
- Или убрать фильтр по категориям
- Или добавлять категорию вручную

### 3. Нет исторических данных для графиков

**Проблема:** API не возвращает историю изменения ставок

**Решение:**
- Генерировать график на основе текущего rate
- Или сделать статичный график
- Или убрать график вообще

### 4. Формат изображений

**Проблема:** API возвращает S3 URLs, могут быть проблемы с CORS

**Решение:**
- Проверить CORS настройки
- Добавить fallback изображения
- Использовать Unsplash API

### 5. Статус события

**Проблема:** API использует статусы PENDING/ACTIVE/CLOSED, а UI ожидает другие

**Решение:**
- Создать маппинг статусов
- Адаптировать UI под API статусы

---

## 📝 Рекомендации

### Приоритет 1: Базовая интеграция

1. ✅ Создать API клиент
2. ✅ Трансформировать данные
3. ✅ Заменить в QuestionCardSection
4. ✅ Добавить loading состояние
5. ✅ Fallback на mock данные

### Приоритет 2: Улучшения

1. 🔄 Кеширование данных
2. 🔄 Обновление в реальном времени
3. 🔄 Детальная страница события
4. 🔄 Фильтрация и сортировка

### Приоритет 3: Оптимизация

1. 📊 Реальные графики (если API добавит историю)
2. 🎯 Категоризация по ML/keywords
3. 💰 Реальные данные о призовом фонде
4. 🔐 Авторизация пользователей

---

## 🛠️ Следующие шаги

### 1. Создать ветку для интеграции

```bash
cd ~/dev/PolyMarket
git checkout -b feature/markets-api-integration
```

### 2. Создать файлы

- `src/lib/marketsAPI.ts` - API клиент
- `src/lib/transformMarkets.ts` - Трансформация данных
- `src/types/markets.ts` - TypeScript типы

### 3. Обновить компоненты

- `QuestionCardSection.tsx` - использовать API
- `EventDetailsPage.tsx` - загрузка по ID

### 4. Тестирование

- Проверить все категории
- Проверить fallback
- Проверить детальные страницы

### 5. Pull Request

- Описание изменений
- Скриншоты до/после
- Checklist функционала

---

## 📊 Текущее состояние

✅ **Проект запущен:** http://localhost:3002  
✅ **Найдены замоканные данные:** 36 событий  
✅ **Структура понятна:** React + TypeScript + Vite  
✅ **API протестирован:** Markets API работает  
✅ **Маппинг спланирован:** Таблица трансформации готова  

**Готов к интеграции! 🚀**

---

## 🎯 Итоги анализа

### ✅ Положительные моменты

- Хорошо структурированный код
- TypeScript типизация
- Компонентная архитектура
- Модульная структура

### ⚠️ Сложности

- Отсутствие некоторых данных в API
- Нужна трансформация структур
- Fallback логика обязательна

### 💡 Возможности

- Легко заменить источник данных
- Можно постепенно мигрировать
- Хорошая база для расширения

---

**Автор анализа:** AI Assistant  
**Дата:** 03 октября 2025  
**Статус:** Готов к интеграции через Pull Request
